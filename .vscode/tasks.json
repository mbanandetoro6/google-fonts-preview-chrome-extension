{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "gulp",
            "task": "serve",
            "isBackground": true,
            "label": "gulp watch files and live serve",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}